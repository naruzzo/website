import { createStitches } from '@stitches/react'

export const { styled, getCssText } = createStitches({
    theme: {
        colors: {
            dark: '#252525',
            light: '#f4f4f4',
        },
    },
})
